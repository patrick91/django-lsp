---
title: Completion examples
description: Executable Django ORM completion scenarios generated by the real language server.
section: Guides
order: 1
---

# Completion examples

This page shows completion menus produced by `django-lsp` against the checked-in Django project
fixture. Every menu is generated by opening the example source through the real JSON-RPC service and
requesting completion at its hidden `<cursor>` marker.

The menu order is the order returned to the editor. Long menus show the first entries and the number
of additional results.

## Model fields

Completion can begin from a partial field name anywhere inside a supported query call.

<!-- django-lsp-example id=model-fields file=blog/views.py limit=8
from .models import Blog

Blog.objects.filter(
    ti<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="model-fields" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Field lookups

After a scalar field and Django's `__` separator, the server offers lookup expressions matching the
typed prefix.

<!-- django-lsp-example id=field-lookups file=blog/views.py limit=8
from .models import Blog

Blog.objects.filter(
    title__<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="field-lookups" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Forward relations

Relations can be traversed without importing or executing the Django project.

<!-- django-lsp-example id=forward-relations file=blog/views.py limit=8
from .models import Blog

Blog.objects.filter(
    author__te<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="forward-relations" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Deep relation paths

Completion continues across multiple related models.

<!-- django-lsp-example id=deep-relation-paths file=blog/views.py limit=8
from .models import Blog

Blog.objects.filter(
    author__team__na<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="deep-relation-paths" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Many-to-many relations

Many-to-many fields expose the related model's fields and lookup paths.

<!-- django-lsp-example id=many-to-many-relations file=blog/views.py limit=8
from .models import Blog

Blog.objects.filter(
    tags__na<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="many-to-many-relations" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Reverse relations

Explicit reverse query names are indexed alongside forward relations.

<!-- django-lsp-example id=reverse-relations file=blog/views.py limit=8
from .models import Author

Author.objects.filter(
    authored_blogs__ti<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="reverse-relations" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Related-object loading

String arguments to `select_related()` only offer single-valued relationships. Completion follows
foreign keys and one-to-one fields across multiple models, while excluding scalar and many-to-many
fields.

<!-- django-lsp-example id=select-related file=blog/views.py limit=8
from .models import Blog

Blog.objects.select_related(
    "author__te<cursor>"
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="select-related" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

`prefetch_related()` offers every relationship type. Reverse relationships use their Python
accessor names, which can differ from the query names used by `filter()`.

<!-- django-lsp-example id=prefetch-related file=blog/views.py limit=8
from .models import Author

Author.objects.prefetch_related(
    "blogs__ta<cursor>"
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="prefetch-related" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->

## Custom user model relations

Relations declared through `settings.AUTH_USER_MODEL` resolve to the configured user model.

<!-- django-lsp-example id=auth-user-model file=blog/views.py limit=8
from .models import Route

Route.objects.filter(
    installer__time<cursor>
)
-->
<!-- django-lsp-output:start -->
<div class="completion-example">
<AutocompleteDemo example="auth-user-model" compact></AutocompleteDemo>
</div>
<!-- django-lsp-output:end -->
